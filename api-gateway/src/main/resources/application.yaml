server:
  port: 8081

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: Auth
          uri: http://localhost:8082
          predicates:
            - Path=/auth/**
          filters:
            - AuthFilter
