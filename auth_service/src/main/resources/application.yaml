server:
  port: 8082

spring:
  application:
    name: auth_service
  datasource:
    url: jdbc:postgresql://localhost:5431/stratos?options=-c%20timezone%3DUTC
    username: postgres
    password: jigar@1612
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql